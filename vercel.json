{
    "buildCommand": "node prebuild.js && prisma generate && prisma db push && prisma migrate dev && next build && cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/",
    "build": {
        "env": {
            "DATABASE_URL": "file:./prisma/dev.db"
        }
    },
    "env": {
        "DATABASE_URL": "file:./prisma/dev.db"
    }
}